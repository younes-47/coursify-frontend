// import { selectUserProfilePageDomain } from '../selectors';

describe('selectUserProfilePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

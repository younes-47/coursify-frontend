// import { selectVerificationPageDomain } from '../selectors';

describe('selectVerificationPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

// import { selectUserMyCoursesPageDomain } from '../selectors';

describe('selectUserMyCoursesPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

// import { selectUserQuizResultDomain } from '../selectors';

describe('selectUserQuizResultDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

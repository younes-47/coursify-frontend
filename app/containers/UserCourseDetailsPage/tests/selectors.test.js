// import { selectUserCourseDetailsPageDomain } from '../selectors';

describe('selectUserCourseDetailsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

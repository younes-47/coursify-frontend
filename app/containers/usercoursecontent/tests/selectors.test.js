// import { selectUserCourseContentDomain } from '../selectors';

describe('selectUserCourseContentDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

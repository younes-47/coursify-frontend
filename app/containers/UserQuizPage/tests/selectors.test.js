// import { selectUserQuizPageDomain } from '../selectors';

describe('selectUserQuizPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

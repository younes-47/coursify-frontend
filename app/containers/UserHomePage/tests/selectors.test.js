// import { selectUserHomePageDomain } from '../selectors';

describe('selectUserHomePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

// import { selectUserEvaluationResultDomain } from '../selectors';

describe('selectUserEvaluationResultDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
